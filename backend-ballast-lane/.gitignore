# ==============================================================================
# RAILS .gitignore - Backend Specific Rules
# ==============================================================================
# This file is portable - if this Rails app moves to its own repo, it works.
# Global rules (IDE, OS, .env) are handled by the root .gitignore.
# ==============================================================================

# ------------------------------------------------------------------------------
# Runtime / Temporary Files
# ------------------------------------------------------------------------------
/log/*
/tmp/*
!/log/.keep
!/tmp/.keep

# ------------------------------------------------------------------------------
# Storage (Active Storage, uploads)
# ------------------------------------------------------------------------------
/storage/*
!/storage/.keep

# ------------------------------------------------------------------------------
# Public Assets (compiled/generated)
# ------------------------------------------------------------------------------
/public/assets
/public/packs
/public/packs-test
.byebug_history

# ------------------------------------------------------------------------------
# Database
# ------------------------------------------------------------------------------
/db/*.sqlite3
/db/*.sqlite3-*

# ------------------------------------------------------------------------------
# Encryption / Credentials (CRITICAL - never commit)
# ------------------------------------------------------------------------------
/config/master.key
/config/credentials/*.key

# ------------------------------------------------------------------------------
# Bundle / Vendor
# ------------------------------------------------------------------------------
/vendor/bundle
.bundle

# ------------------------------------------------------------------------------
# Node (if using webpacker/jsbundling)
# ------------------------------------------------------------------------------
/node_modules
/yarn-error.log
yarn-debug.log*
.yarn-integrity

# ------------------------------------------------------------------------------
# Coverage / Test artifacts
# ------------------------------------------------------------------------------
/coverage/
/spec/examples.txt

# ------------------------------------------------------------------------------
# Spring (dev server preloader)
# ------------------------------------------------------------------------------
/tmp/pids/*
!/tmp/pids/.keep
spring/*.pid

# ------------------------------------------------------------------------------
# Bootsnap cache
# ------------------------------------------------------------------------------
/tmp/cache/bootsnap*
